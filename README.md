This is a demo for a sales prediction project using FBProphet.

# Installation:

Clone the repository and use the command 
```bash
cd "project path"
pip install -r requirements.txt
streamlit run main.py
```
